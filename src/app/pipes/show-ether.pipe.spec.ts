import { ShowEtherPipe } from './show-ether.pipe';

describe('ShowEtherPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowEtherPipe();
    expect(pipe).toBeTruthy();
  });
});
